<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pammmuse_admin.adminservice.dao.ProductDao">

    <!--제품 등록-->
    <insert id="productEnroll">
        insert into product(product_name, cate_code, product_price, product_stock, product_color, product_size, product_discount)
        values(#{product_name}, #{cate_code}, #{product_price}, #{product_stock}, #{product_color}, #{product_size}, #{product_discount})

    </insert>


</mapper>